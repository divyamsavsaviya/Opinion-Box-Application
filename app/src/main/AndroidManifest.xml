<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalopinionbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GetInfoActivity" />
        <activity android:name=".AddEventOptionActivity" />
        <activity android:name=".AccountMainActivity" />
        <activity android:name=".TargetFilterActivity" />
        <activity android:name=".AddPollOptionActivity" />
        <activity android:name=".EventMainActivity" />
        <activity android:name=".AddPostMainActivity" />
        <activity android:name=".PostMainActivity" />
        <activity android:name=".Main_Menue_Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>